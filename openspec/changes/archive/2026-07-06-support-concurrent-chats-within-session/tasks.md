## 1. 并发边界回归测试

- [x] 1.1 为同会话不同 `chat_file` 的并发聊天补充后端测试，锁定“可并发且各自只写回目标聊天文件”的行为
- [x] 1.2 为同一 `chat_file` 的并发请求补充后端测试，锁定“仍然串行且不会写坏聊天 JSON”的行为
- [x] 1.3 为 `active_chat` 被并发更新时的显式 `chat_file` 请求补充测试，锁定“不会被元数据改写目标聊天”的行为

## 2. 后端锁域拆分与聊天语义收敛

- [x] 2.1 在 `SessionManager` 中拆分会话配置锁与聊天文件锁，并提供统一的锁获取辅助路径
- [x] 2.2 调整 `chat()` 与 `chat_stream()`，使显式 `chat_file` 成为请求真实目标，不再依赖会话级唯一当前聊天
- [x] 2.3 梳理 `new_chat`、`select_chat`、聊天预览更新与配置写回路径，统一锁顺序并缩短会话配置锁持有时间

## 3. 会话元数据与接口契约调整

- [x] 3.1 收敛 `active_chat` 语义为兼容性的最近选择元数据，并清理聊天请求对它的硬依赖
- [x] 3.2 调整会话 API 契约与实现，确保 `/api/session/chat` 和 `/api/session/chat/stream` 围绕显式 `chat_file` 工作
- [x] 3.3 复核会话详情、选聊和新建聊天相关响应，确保兼容字段仍可返回但不再主导聊天执行

## 4. 前端页面本地当前聊天状态

- [x] 4.1 调整会话页状态管理，改为在页面本地维护当前选中的 `chat_file`
- [x] 4.2 调整发送消息、新建聊天、切换聊天逻辑，确保请求始终携带本地选中的 `chat_file`
- [x] 4.3 复核多页面同会话场景，确保一个页面的选聊变化不会强制覆盖另一个页面的当前聊天视图

## 5. 验证与收口

- [x] 5.1 运行后端自动化测试，验证同会话不同聊天并发、同一聊天串行与 `active_chat` 降级语义均通过
- [x] 5.2 运行前端构建或相关测试，验证本地选聊状态调整未破坏会话页交互
- [x] 5.3 复核 proposal、design、specs、tasks 一致性，确认该变更已达到可进入 apply/implementation 的状态
