## 1. 后端运行时日志

- [x] 1.1 审计后端运行时输出，移除残留的默认 `[TIMING]`、`@timed`、`get_stats` 和 `/api/performance` 行为。
- [x] 1.2 使用 Python 标准库 `logging` 增加一个轻量后端日志配置工具，日志级别由本地配置或环境变量控制。
- [x] 1.3 将后端启动和模型初始化失败输出改为使用后端 logger，同时保留清晰可见的错误信息。
- [x] 1.4 确认默认模式下，普通成功的后端 API 请求不会输出应用层耗时日志。

## 2. 前端 API 日志

- [x] 2.1 增加一个 Vite 环境变量开关，用于控制前端 API debug 日志。
- [x] 2.2 更新 `ui/src/api/index.ts`，让成功请求耗时和慢请求警告只在 debug 开关启用时输出。
- [x] 2.3 保持前端请求失败始终通过 `console.error` 可见，不受 debug 开关影响。
- [x] 2.4 分别验证 debug 关闭和开启时的浏览器 Console 行为。

## 3. 日志位置与文档

- [x] 3.1 更新 README 的日志和调试章节，说明后端终端日志、`logs/dev-backend.*.log`、前端终端日志、`logs/dev-frontend.*.log`、浏览器 Console/Network 面板和检索诊断报告的位置。
- [x] 3.2 从文档化流程中移除或弱化根目录临时日志文件。
- [x] 3.3 明确说明检索诊断仍然是显式命令，不会在普通聊天请求中默认输出。
- [x] 3.4 记录前端和后端 debug 开关，并给出本地运行示例命令。

## 4. 验证

- [x] 4.1 日志改动完成后，运行后端编译或导入检查。
- [x] 4.2 在前端运行 `npm run build`。
- [x] 4.3 本地启动后端和前端，验证 `GET /api/health` 仍然成功。
- [x] 4.4 触发一次前端 API 失败，确认浏览器 Console 中仍然能看到错误。
- [x] 4.5 扫描代码库和文档化日志路线，确认不再保留默认 `[TIMING]` 要求。
