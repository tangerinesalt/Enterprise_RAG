## 1. 路径安全边界

- [x] 1.1 为知识库与会话模块实现统一的受限路径解析与根目录 containment 校验
- [x] 1.2 为知识库名、会话名和聊天文件名补充合法性校验，明确覆盖空白名、尾随点/空格、Windows 保留设备名和控制字符，并在 API/CLI 入口统一返回稳定错误
- [x] 1.3 将知识库上传、上传并索引、文件删除、聊天读取与聊天删除路径全部切换到受限路径解析逻辑
- [x] 1.4 为 Web 上传端点实现“先裁剪叶子文件名，再校验叶子文件名”的统一策略

## 2. 检索缓存一致性

- [x] 2.1 调整 BM25 缓存键或失效机制，使其基于语料版本而非仅基于 chunk 数量
- [x] 2.2 在索引、重建索引和删除向量后的查询路径上验证 BM25 语料刷新行为
- [x] 2.3 补充混合检索回归测试，覆盖“chunk 数量不变但文本变化”场景

## 3. 聊天持久化契约统一

- [x] 3.1 统一同步聊天与流式聊天的助手消息持久化结构，使用 `content` + `additional_kwargs.sources`
- [x] 3.2 调整历史消息读取与返回结构，确保来源信息从持久化消息中稳定暴露
- [x] 3.3 补充同步/流式聊天一致性测试，验证历史回放不再依赖不同解析逻辑
- [x] 3.4 验证旧同步聊天记录在无结构化来源时按“仅正文可读”降级，而不是反解析正文补来源

## 4. 前端会话页适配

- [x] 4.1 更新会话页面历史消息恢复逻辑，只从结构化持久化消息恢复来源信息
- [x] 4.2 调整来源展示逻辑，确保同步聊天与流式聊天历史呈现一致
- [ ] 4.3 验证刷新页面、切换聊天和重新加载历史后的来源面板行为
- [ ] 4.4 验证旧聊天记录在前端按“正文可读、来源面板缺失”稳定降级展示

## 5. 回归与收尾

- [x] 5.1 增补 API、模块和前端相关测试，覆盖非法路径、越界拒绝、Windows 保留名称、叶子文件名裁剪/拒绝、检索缓存刷新和历史来源恢复
- [ ] 5.2 运行项目测试与关键手工回归，确认知识库管理、会话聊天和来源展示未回退
- [x] 5.3 更新必要文档，说明新的路径约束、错误行为和聊天历史契约
